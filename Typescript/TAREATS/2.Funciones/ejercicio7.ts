// Crea una función llamada calcularAreaTriangulo
// que tome la base y la altura de un triángulo como parámetros y devuelva su área.
function calcularAreaTriangulo(base: number, altura: number): number {
  return (base * altura) / 2;
}
