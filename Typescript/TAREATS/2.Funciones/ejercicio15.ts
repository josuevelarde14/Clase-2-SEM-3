// Crea una función llamada invertirCadena que tome una cadena
//  de texto como parámetro y devuelva la cadena invertida.
function invertirCadena(cadena: string): string {
  return cadena.split("").reverse().join("");
}
