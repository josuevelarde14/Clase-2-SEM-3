// Crea una función llamada encontrarNumeroMaximo que tome un número
// variable de parámetros y devuelva el número máximo.
function encontrarNumeroMaximo(...numeros: number[]): number {
  return Math.max(...numeros);
}
