// •	Crea un módulo separado con una función para calcular el área de un triángulo. 
// Importa la función en tu archivo principal
//  y calcula el área de un triángulo utilizando la función.

export function calcularAreaTriangulo(base: number, altura: number): number {
    return (base * altura) / 2;
}
