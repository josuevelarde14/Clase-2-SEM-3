// Crea una función llamada calcularPotencia que tome dos números como parámetros y
//  devuelva el resultado de elevar el primero al segundo.
function calcularPotencia(base: number, exponente: number): number {
  return Math.pow(base, exponente);
}
