// Crea una función llamada
// obtenerMayor que tome dos números como parámetros y devuelva el mayor de ellos.
function obtenerMayor(num1: number, num2: number): number {
  return Math.max(num1, num2);
}
