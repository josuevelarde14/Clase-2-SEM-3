// Crea una función llamada
// generarNumeroAleatorio que genere y devuelva un número aleatorio entre 1 y 100.
function generarNumeroAleatorio(): number {
  return Math.floor(Math.random() * 100) + 1;
}
