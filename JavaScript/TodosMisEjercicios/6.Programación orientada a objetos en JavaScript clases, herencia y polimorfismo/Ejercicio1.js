// Crea una clase llamada Animal con un método hacerSonido que imprima en la
// consola el sonido que hace el animal. Luego crea una instancia de la clase y llama al
// método hacerSonido
class Animal {
  hacerSonido() {
    console.log("El animal hace un sonido");
  }
}

const animal = new Animal();
animal.hacerSonido(); // Imprime "El animal hace un sonido"
